{
  "name": "@test-graneet/db",
  "scripts": {
    "dev": "pnpm start",
    "start": "docker-compose up -d db",
    "reset": "docker-compose stop db && docker-compose rm && rimraf ./tmp && pnpm start"
  }
}
